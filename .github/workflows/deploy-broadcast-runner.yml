name: Deploy broadcast-runner

# Path-filtered per-function deploy. Additive - closes a gap where
# broadcast-runner (like every function importing _shared) had nothing
# redeploying it on a _shared/** change: it isn't on deploy.yml's
# unconditional list (deliberately left alone here - see task-4-report.md
# for why that list wasn't touched) and had no dedicated workflow of its
# own, same as five other functions fixed alongside this one.
#
# broadcast-runner is invoked by pg_cron with no Authorization header and
# fail-closes on x-queue-worker-token - see supabase/config.toml's
# verify_jwt = false entry for it and task-4-report.md's fix-report section
# for how that was confirmed live.
on:
  push:
    branches: [ 'main' ]
    paths:
      - 'supabase/functions/broadcast-runner/**'
      - 'supabase/functions/_shared/**'
      - 'supabase/config.toml'
      - '.github/workflows/deploy-broadcast-runner.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy broadcast-runner Edge Function
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install deps
        run: npm ci --ignore-scripts

      - name: Deploy broadcast-runner
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          # The supabase CLI is a devDependency, so it lives in node_modules/.bin,
          # which Actions does not put on PATH for `run:` steps. Every invocation
          # must go through npx or it fails with "supabase: command not found".
          npx supabase --version
          npx supabase login --token "$SUPABASE_ACCESS_TOKEN"
          npx supabase functions deploy broadcast-runner --project-ref "$SUPABASE_PROJECT_REF" --use-api --yes
